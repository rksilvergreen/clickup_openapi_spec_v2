description: "Bad request - Name is required, or is already taken"
content:
  application/json:
    schema:
      type: object
      properties:
        error:
          type: string
          example: "Name is required"
        ECODE:
          type: string
          example: OAUTH_117
