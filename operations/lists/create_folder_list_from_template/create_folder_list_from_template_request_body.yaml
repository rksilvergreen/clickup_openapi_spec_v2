required: true
content:
  application/json:
    schema:
      type: object
      required:
        - name
      properties:
        name:
          type: string
          description: "Name of the new List"
        options:
          $ref: "../../../shared/options_2.yaml"
